// Test the path resolution
import { generateSignature } from '../shared/generateSignature'
import { defaultModule } from '../shared/cknTypes/types'

console.log('Path resolution test', { generateSignature, defaultModule })
export {} // Make it a module